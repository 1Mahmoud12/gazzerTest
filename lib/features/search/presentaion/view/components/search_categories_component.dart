import 'package:flutter/material.dart';

class SearchCategoriesComponent extends StatelessWidget {
  const SearchCategoriesComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}