import 'package:flutter/widgets.dart';

class AppConst {
  static final navKey = GlobalKey<NavigatorState>();
  static final defaultRadius = 32.0;
  static final defaultBorderRadius = BorderRadius.circular(defaultRadius);
}
