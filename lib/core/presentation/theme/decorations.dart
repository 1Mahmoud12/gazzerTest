import 'package:flutter/material.dart';

class AppDec {
  const AppDec._();
  // static const LinearGradient gradient =
  static List<BoxShadow> get blackTextShadow => [
    const BoxShadow(color: Color(0xFF9E9E9E), offset: Offset(0, 2), blurRadius: 2),
  ];
}
